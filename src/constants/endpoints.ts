const enum Endpoints {
  root = '/',
  signup = '/signup',
  signin = '/signin',
  signout = '/signout',
  current = '/current',
  restorePass = '/restore-password',
  dynamicId = 'dynamicId',
}

export default Endpoints;
